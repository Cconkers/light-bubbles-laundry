---
/**
 * Contenedor de tarjeta reutilizable.
 * Variantes: plain | tinted | featured | sunken.
 * Radio radius-card, sombra shadow-sm. Sin esquinas vivas.
 */
interface Props {
  variant?: 'plain' | 'tinted' | 'featured' | 'sunken';
  interactive?: boolean;
  href?: string;
  ariaLabel?: string;
  class?: string;
}

const {
  variant = 'plain',
  interactive = false,
  href,
  ariaLabel,
  class: cls = '',
} = Astro.props;

const isLink = Boolean(href);

const base = [
  'rounded-card',
  'transition-shadow duration-[var(--duration-base)]',
  isLink
    ? 'no-underline text-inherit focus-visible:outline-none focus-visible:shadow-focus'
    : '',
].join(' ');

const variantStyles: Record<string, string> = {
  plain:    'bg-surface shadow-sm border border-border',
  tinted:   'bg-surface-sunken border border-border',
  featured: 'bg-surface shadow-sm border-2 border-accent',
  sunken:   'bg-surface-sunken',
};

const classes = [
  base,
  variantStyles[variant],
  interactive || isLink ? 'cursor-pointer hover-lift' : '',
  cls,
].filter(Boolean).join(' ');
---

{
  href ? (
    <a href={href} class={classes} aria-label={ariaLabel}>
      <slot />
    </a>
  ) : (
    <div class={classes}>
      <slot />
    </div>
  )
}
